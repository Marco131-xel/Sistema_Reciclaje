# crear proyecto para react
npm create vite@latest fronted_SR
cd fronted_SR
npm install
npm run dev

    # LARAVEL
# para crear controllers
php artisan make:controller EmpleadoController

# para crear models y migracion
php artisan make:model Empleado -m

# para compilar tu migraciones
php artisan migrate

# configuracion cors
php artisan config:publish cors

# insert de rol para la base de datos
INSERT IGNORE INTO rol (nombre) VALUES
('administrador_municipal'),
('coordinador_rutas'),
('operador_punto_verde'),
('ciudadano'),
('auditor');

#borrar y empezar el contador en 0
TRUNCATE TABLE usuario_rol;
TRUNCATE TABLE users;